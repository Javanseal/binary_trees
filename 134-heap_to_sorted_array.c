#include "binary_trees.h"
/**
 * heap_to_sorted_array - to convert a Binary Max Heap to a sorted array of int
 * @heap: the root of the tree
 * @size: the size from array
 * Return: pointer
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	return (NULL);
}
